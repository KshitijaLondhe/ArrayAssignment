using System;
//5.WAP to half sort the array in ascending and half in descending


namespace ArrayAssignment
{
    public class HalfArraySorting
    {
        /*static void Main(string[] args)
        {
            //5.WAP to half sort the array in ascending and half in descending
            int[] a = {12,77,33,99,44,10,5,49,2 ,65};
            Console.WriteLine("Array");
            foreach (int a1 in a)
            {
                Console.WriteLine(a1);
            }
            int mid = a.Length / 2;
            Array.Sort(a, 0, mid);

            //sort half part
            Array.Sort(a, mid, a.Length - mid);
            // Array.Reverse(a, mid, a.Length - mid);
            Console.WriteLine("Array after sorting");
            foreach (int a1 in a)
            {
                Console.WriteLine(a1);
            }
        }*/
    }
}
